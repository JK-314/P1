class AppConstants {
  AppConstants._();

  static const String appName = 'Eatsence';

  static const int matchingStepCount = 3;

  static const Duration animationDuration = Duration(milliseconds: 300);
  static const Duration snackBarDuration = Duration(seconds: 3);
}
